<Page
    x:Class="Gogh_alpha.ImageCropper"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Gogh_alpha"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource SystemControlChromeMediumAcrylicWindowMediumBrush}">
    <Page.Resources>
        <ControlTemplate x:Key="ImageCropperTemplate1" TargetType="controls:ImageCropper">
            <Grid/>
        </ControlTemplate>
    </Page.Resources>

    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition />

        </Grid.RowDefinitions>
        <Image Source="/Assets/gogh.png" Grid.Row="2" x:Name="transition" ></Image>
        <controls:ImageCropper   x:Name="imageCropper" Grid.Row="2" CropShape="Rectangular"
                           ThumbPlacement="All"  Background="Gray" />
        
        <AppBarButton x:Name="back"  Icon="Back"  Click="AppBarButton_Click"/>
        <TextBlock x:Name="txt" Grid.Row="1" Text="adsklfhskajfhd" HorizontalAlignment="Center"></TextBlock>
    </Grid>
</Page>
